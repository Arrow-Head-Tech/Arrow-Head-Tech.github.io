# Mission Control contract for this repo. Declarative SYNC file — MC reconciles it into live
# state on ingestion (it PROPOSES; a manual board action always prevails).
# Generated by Mission Control (F-008-007). Adjust deploy.type / stage / add resources as needed.
# deploy.type corrected to 'static' on review (2026-09-02): the package.json is a build step,
# there is no start script — this is a GitHub Pages site, served as static files.
version: 1
stage: prototype
deploy:
  type: static
