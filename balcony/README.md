# BALCONY

Scanner de pasta local → export para `content/projects.json` (formato do hub).

**Status:** Placeholder. Implementação prevista na Fase 3 do plano.

- Configurar pasta de projetos (fora do repo) em config ou `.env`.
- Rodar `python scan_and_export.py` para gerar/atualizar `content/projects.json`.
- Mapeamento pastas → phase (ex.: 1.IDEA → idea) conforme plano.
