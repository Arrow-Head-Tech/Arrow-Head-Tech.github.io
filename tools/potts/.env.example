# GitHub token (required for POTTS when creating repos or listing org repos).
# Create at: GitHub → Settings → Developer settings → Personal access tokens
# Scopes needed: repo, read:org (and workflow if using Actions)
GITHUB_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxx
